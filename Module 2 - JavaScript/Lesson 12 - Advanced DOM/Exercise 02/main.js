"use strict";

const images = [{ name: "Madrid", img: "./img/madrig.jpg" }];
const images = [{ name: "París", img: "./img/paris.jpeg" }];
const images = [{ name: "NY", img: "./img/NY.jpg" }];
const form = document.querySelector(".js-form");

const input = document.createElement("input");
form.appendChild(input);
